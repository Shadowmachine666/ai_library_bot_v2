# План полного тестирования AI Library Bot

## Цель
Проверить работоспособность всех компонентов бота в mock-режиме перед переходом к реальным API.

---

## Этап 1: Автоматические тесты (Unit Tests)

### 1.1 Запуск всех тестов

```powershell
cd ai_library_bot
python -m pytest tests/ -v
```

**Ожидаемый результат:**
- Все тесты проходят успешно
- Покрытие основных сценариев

### 1.2 Проверка покрытия тестами

```powershell
python -m pytest tests/ --cov=src --cov-report=term-missing
```

**Что проверить:**
- ✅ Тесты для `ingest_service.py`
- ✅ Тесты для `retriever_service.py`
- ✅ Тесты для `analyzer.py`
- ✅ Тесты для `formatters.py`
- ✅ Тесты для `telegram_bot.py` (flow)

---

## Этап 2: Проверка конфигурации

### 2.1 Проверка переменных окружения

```powershell
# Проверьте, что файл .env существует
Test-Path ai_library_bot\.env

# Проверьте содержимое (не должно быть реальных токенов в mock-mode)
Get-Content ai_library_bot\.env
```

**Что проверить:**
- ✅ Файл `.env` существует
- ✅ `TG_TOKEN` установлен (можно использовать тестовый токен)
- ✅ `OPENAI_API_KEY` установлен (можно использовать placeholder)
- ✅ Все остальные переменные имеют значения по умолчанию

### 2.2 Валидация конфигурации

```powershell
cd ai_library_bot
python -c "from src.config import Config; print('Config OK' if Config.validate() else 'Config ERROR')"
```

**Ожидаемый результат:**
- `Config OK` или предупреждения о mock-режиме

---

## Этап 3: Тестирование Ingestion (Загрузка книг)

### 3.1 Подготовка тестовых данных

**Требования к книгам:**
- Минимум 2 файла в формате `.txt`
- Каждый файл должен содержать минимум 200 символов
- Рекомендуется: 1000+ символов для тестирования чанкинга

### 3.2 Запуск индексации

```powershell
cd ai_library_bot
python -m src.main ingest --folder data/books
```

**Что проверить в логах:**
- ✅ Найдены все файлы (без дубликатов)
- ✅ Каждый файл успешно обработан
- ✅ Созданы чанки (минимум 1 чанк на файл)
- ✅ Нет ошибок
- ✅ MOCK сообщения о сохранении в FAISS

**Ожидаемый вывод:**
```
INFO - Начало индексации книг из папки: data/books
INFO - Найдено 2 файлов для обработки
INFO - Обработка файла: data/books/book1.txt
INFO - Создано X чанков из текста длиной Y символов
INFO - MOCK: Индекс будет сохранён в data/index.faiss
INFO - Файл успешно обработан: X чанков
...
INFO - Индексация завершена: обработано 2 файлов, ошибок 0
```

---

## Этап 4: Тестирование Telegram бота (Mock Mode)

### 4.1 Запуск бота

**ВАЖНО:** Для тестирования нужен реальный Telegram Bot Token (можно получить у @BotFather).

```powershell
cd ai_library_bot
python -m src.main run
```

**Ожидаемый вывод:**
```
INFO - Создание приложения Telegram бота...
INFO - Обработчики зарегистрированы: /start, текстовые сообщения
INFO - Бот запущен и готов к работе
INFO - Ожидание сообщений...
```

### 4.2 Тестирование через Telegram

#### Сценарий 1: Команда /start

**Действие:** Отправьте `/start` боту в Telegram

**Ожидаемый результат:**
- ✅ Бот отвечает приветственным сообщением
- ✅ Сообщение содержит инструкции
- ✅ Форматирование Markdown работает

**Проверка логов:**
```
INFO - Команда /start от пользователя X (@username)
```

---

#### Сценарий 2: Успешный запрос (SUCCESS)

**Действие:** Отправьте вопрос, который должен найти информацию в загруженных книгах

**Примеры вопросов:**
- "Что такое Python?" (если в книгах есть информация о Python)
- "Расскажи о машинном обучении" (если в книгах есть ML)
- Любой вопрос, связанный с содержимым загруженных книг

**Ожидаемый результат:**
- ✅ Бот отвечает "🔍 Ищу информацию..."
- ✅ Затем отправляет ответ с:
  - Статусом SUCCESS
  - Текстом ответа
  - Цитатами из источников
  - Disclaimer

**Проверка логов:**
```
INFO - Запрос от пользователя X: [вопрос]
INFO - MOCK: Поиск релевантных чанков для запроса
INFO - MOCK: Найдено X релевантных чанков
INFO - MOCK: Вызов LLM, модель=gpt-4o-mini
INFO - Ответ успешно отправлен пользователю X
```

---

#### Сценарий 3: Информация не найдена (NOT_FOUND)

**Действие:** Отправьте вопрос, на который нет ответа в загруженных книгах

**Примеры:**
- "Как работает квантовая физика?" (если в книгах нет такой информации)
- "Расскажи о космосе" (если книги не о космосе)
- Любой вопрос, не связанный с содержимым книг

**Ожидаемый результат:**
- ✅ Бот отвечает сообщением "Информация не найдена"
- ✅ Предлагает переформулировать вопрос

**Проверка логов:**
```
INFO - Запрос от пользователя X: [вопрос]
WARNING - Не найдено релевантных чанков для запроса: [вопрос]
```

---

#### Сценарий 4: Требуется уточнение (CLARIFICATION_NEEDED)

**Действие:** Отправьте слишком общий вопрос

**Примеры:**
- "Что это?"
- "Расскажи"
- "Объясни"

**Ожидаемый результат:**
- ✅ Бот отвечает, что вопрос слишком общий
- ✅ Предлагает уточняющий вопрос

**Примечание:** В mock-режиме этот сценарий может не сработать, так как mock LLM всегда возвращает SUCCESS. Это нормально.

---

#### Сценарий 5: Конфликт данных (CONFLICT)

**Действие:** Отправьте вопрос, на который в книгах есть противоречивая информация

**Ожидаемый результат:**
- ✅ Бот сообщает о конфликте
- ✅ Показывает противоречивые источники

**Примечание:** В mock-режиме этот сценарий может не сработать. Это нормально.

---

#### Сценарий 6: Кэширование

**Действие:** 
1. Отправьте вопрос (например, "Что такое Python?")
2. Дождитесь ответа
3. Отправьте тот же вопрос снова

**Ожидаемый результат:**
- ✅ Второй ответ приходит быстрее (из кэша)
- ✅ Ответ идентичен первому

**Проверка логов:**
```
INFO - Запрос от пользователя X: [вопрос]
INFO - Ответ найден в кэше для запроса: [вопрос]
```

---

#### Сценарий 7: Слишком длинный запрос

**Действие:** Отправьте запрос длиннее 1000 символов

**Ожидаемый результат:**
- ✅ Бот отвечает ошибкой: "Запрос слишком длинный. Пожалуйста, ограничьте его 1000 символами."

---

#### Сценарий 8: Обработка ошибок

**Действие:** Попробуйте различные edge cases:
- Пустое сообщение
- Сообщение только из пробелов
- Специальные символы

**Ожидаемый результат:**
- ✅ Бот корректно обрабатывает все случаи
- ✅ Нет падений бота
- ✅ Ошибки логируются

---

## Этап 5: Проверка компонентов по отдельности

### 5.1 Тестирование Retriever Service

```powershell
cd ai_library_bot
python -c "
import asyncio
from src.retriever_service import retrieve_chunks, NOT_FOUND

async def test():
    # Тест 1: Успешный поиск
    result = await retrieve_chunks('Python')
    print(f'Результат поиска: {type(result)}')
    if result != NOT_FOUND:
        print(f'Найдено чанков: {len(result)}')
    else:
        print('NOT_FOUND')
    
    # Тест 2: Не найдено
    result2 = await retrieve_chunks('абсолютно_несуществующий_запрос_xyz123')
    print(f'Результат 2: {result2}')

asyncio.run(test())
"
```

**Ожидаемый результат:**
- ✅ Mock retriever возвращает mock-данные
- ✅ NOT_FOUND работает корректно

---

### 5.2 Тестирование Analyzer

```powershell
cd ai_library_bot
python -c "
import asyncio
from src.analyzer import analyze

async def test():
    mock_chunks = [
        {'text': 'Python - это язык программирования', 'source': 'book1.txt'},
        {'text': 'Python прост в изучении', 'source': 'book1.txt'}
    ]
    result = await analyze(mock_chunks, 'Что такое Python?')
    print(f'Статус: {result.status}')
    if result.result:
        print(f'Ответ: {result.result.answer[:100]}...')

asyncio.run(test())
"
```

**Ожидаемый результат:**
- ✅ Analyzer возвращает AnalysisResponse
- ✅ JSON валидируется через Pydantic
- ✅ Mock LLM возвращает валидный ответ

---

### 5.3 Тестирование Formatters

```powershell
cd ai_library_bot
python -c "
from src.analyzer import AnalysisResponse, Result, Quote
from src.formatters import format_response

# Тест SUCCESS
result = AnalysisResponse(
    status='SUCCESS',
    clarification_question=None,
    result=Result(
        answer='Python - это язык программирования',
        quotes=[Quote(text='Python прост', source='book1.txt')],
        disclaimer='Это анализ на основе загруженных текстов.'
    )
)
print(format_response(result))
"
```

**Ожидаемый результат:**
- ✅ Форматирование работает корректно
- ✅ Markdown синтаксис правильный
- ✅ Все статусы обрабатываются

---

## Этап 6: Проверка логов

### 6.1 Проверка файлов логов

```powershell
# Проверьте, что логи создаются
Test-Path ai_library_bot\logs\*.log

# Просмотрите последние логи
Get-Content ai_library_bot\logs\*.log -Tail 50
```

**Что проверить:**
- ✅ Логи создаются в папке `logs/`
- ✅ Уровни логирования правильные (INFO, DEBUG, WARNING, ERROR)
- ✅ Логи содержат полезную информацию
- ✅ Нет критических ошибок

---

## Этап 7: Проверка производительности (Mock Mode)

### 7.1 Время ответа

**Действие:** Отправьте несколько запросов и замерьте время ответа

**Ожидаемый результат:**
- ✅ Ответ приходит в разумное время (< 5 секунд в mock-mode)
- ✅ Кэшированные запросы отвечают быстрее

---

## Этап 8: Финальная проверка

### 8.1 Чек-лист готовности

- [ ] Все автоматические тесты проходят
- [ ] Ingestion работает корректно
- [ ] Бот запускается без ошибок
- [ ] Команда /start работает
- [ ] Успешные запросы обрабатываются
- [ ] NOT_FOUND сценарий работает
- [ ] Кэширование работает
- [ ] Обработка ошибок работает
- [ ] Логи пишутся корректно
- [ ] Нет критических ошибок в логах

---

## Следующие шаги

После успешного прохождения всех тестов:

1. **Переход к реальным API:**
   - Получить реальный OpenAI API ключ
   - Получить реальный Telegram Bot Token
   - Выполнить команду `ENABLE_REAL_API` (когда будет реализовано)

2. **Тестирование с реальными API:**
   - Повторить все сценарии с реальными API
   - Проверить качество ответов
   - Проверить стоимость API вызовов

3. **Оптимизация:**
   - Настройка параметров чанкинга
   - Настройка параметров поиска
   - Оптимизация промптов

---

## Отчёт о тестировании

После завершения тестирования заполните отчёт:

```
Дата тестирования: [дата]
Версия: [версия]
Тестировщик: [имя]

Результаты:
- Автоматические тесты: [PASSED/FAILED]
- Ingestion: [PASSED/FAILED]
- Telegram бот: [PASSED/FAILED]
- Все сценарии: [PASSED/FAILED]

Найденные проблемы:
1. [описание]
2. [описание]

Рекомендации:
1. [рекомендация]
2. [рекомендация]
```





