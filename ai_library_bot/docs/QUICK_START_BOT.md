# Быстрый старт: Запуск Telegram бота

## Шаг 1: Получение Telegram Bot Token

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "My Library Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `my_library_bot`)
4. BotFather отправит вам токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Скопируйте токен** - он понадобится в следующем шаге

## Шаг 2: Настройка .env файла

### Вариант A: Если файл .env уже существует

Откройте файл `ai_library_bot/.env` и добавьте/обновите строку:

```
TG_TOKEN=ваш_токен_от_BotFather
```

### Вариант B: Создание нового .env файла

Создайте файл `ai_library_bot/.env` со следующим содержимым:

```
TG_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=placeholder_for_mock_mode
FAISS_PATH=./data/index.faiss
CACHE_BACKEND=memory
LOG_LEVEL=INFO
```

**Важно:** 
- Замените `ваш_токен_от_BotFather` на реальный токен, полученный от @BotFather
- `OPENAI_API_KEY` может быть любым значением в mock-режиме (например, `placeholder`)

## Шаг 3: Запуск бота

```powershell
cd ai_library_bot
python -m src.main run
```

**Ожидаемый вывод:**
```
INFO - Создание приложения Telegram бота...
INFO - Обработчики зарегистрированы: /start, текстовые сообщения
INFO - Бот запущен и готов к работе
INFO - Ожидание сообщений...
```

Если видите ошибку о `TG_TOKEN`, проверьте:
- Файл `.env` существует в папке `ai_library_bot/`
- Токен правильно скопирован (без лишних пробелов)
- Токен начинается с цифр и содержит двоеточие

## Шаг 4: Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота по username (который вы указали при создании)
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

## Шаг 5: Тестирование запросов

### Тест 1: Успешный запрос
Отправьте вопрос, связанный с содержимым загруженных книг:
```
Что такое Python?
```

**Ожидаемый результат:**
- Бот отвечает "🔍 Ищу информацию..."
- Затем отправляет ответ с информацией (в mock-режиме будет mock-ответ)

### Тест 2: Запрос без информации
Отправьте вопрос, на который нет ответа в книгах:
```
Как работает квантовая физика?
```

**Ожидаемый результат:**
- Бот отвечает "Информация не найдена"

### Тест 3: Кэширование
Отправьте тот же вопрос дважды:
1. Первый раз - бот обрабатывает запрос
2. Второй раз - бот отвечает быстрее (из кэша)

## Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот.

## Устранение проблем

### Ошибка: "TG_TOKEN не установлен"
- Проверьте, что файл `.env` находится в папке `ai_library_bot/`
- Проверьте, что токен правильно указан в файле
- Убедитесь, что нет лишних пробелов вокруг токена

### Ошибка: "Unauthorized" или "Invalid token"
- Проверьте, что токен скопирован полностью
- Убедитесь, что токен не содержит лишних символов
- Попробуйте создать нового бота через @BotFather

### Бот не отвечает
- Проверьте логи в терминале
- Убедитесь, что бот запущен (видите "Ожидание сообщений...")
- Проверьте, что вы пишете правильному боту

## Следующие шаги

После успешного тестирования в mock-режиме:
1. Переходите к реальным API (когда будет команда `ENABLE_REAL_API`)
2. Настройте реальный OpenAI API ключ
3. Загрузите реальные книги в индекс




