# AI Library Bot

Telegram-бот с RAG (Retrieval-Augmented Generation) для ответов на вопросы по загруженным книгам.

## Возможности

### Основные функции
- 📚 Поддержка форматов: TXT, PDF, EPUB, FB2
- 🔍 Векторный поиск по содержимому книг (FAISS)
- 🤖 Генерация ответов на основе найденных фрагментов (OpenAI GPT-4o-mini)
- ⚡ Асинхронная архитектура
- 🔄 Инкрементальная индексация (только новые/измененные файлы)
- 💾 Кэширование результатов запросов с автоматической очисткой при изменении книг
- 🎯 Умная фильтрация релевантных фрагментов для оптимизации токенов

### Система категорий
- 🏷️ **8 категорий книг**: бизнес, маркетинг, психология, социология, политология, инвестирование, менеджмент, экономика
- 🤖 **Автоматическое определение категорий** через LLM при индексации
- 📝 **Парсинг категорий из названий файлов** (формат: `Название книги (категория1, категория2).pdf`)
- ✅ **Подтверждение категорий администратором** через Telegram
- 🔍 **Фильтрация поиска по категориям** - пользователи могут выбирать категории для поиска
- 📌 **Множественный выбор категорий** - выбор нескольких категорий для одного запроса
- 🎯 **Автоопределение категорий для запросов** - LLM автоматически определяет релевантные категории

### Автоматизация
- 🔔 **Автоматическое обнаружение новых книг** - бот проверяет папку каждые 10 минут
- 📨 **Уведомления администратору**:
  - О новых непроиндексированных книгах
  - О необходимости подтверждения категорий
  - Об успешной индексации книг
- 🧹 **Автоматическая очистка** старых запросов (старше 1 дня)
- 🗑️ **Автоматическое удаление** книг из индекса при удалении файлов
- 🗑️ **Автоматическое удаление** несуществующих файлов из списка ожидания
- 📋 **Автоматическое обновление каталога библиотеки** (`library_catalog.txt`) при изменении книг
- 🔧 **Восстановление индекса** из метаданных при повреждении FAISS индекса

## Требования

- Python 3.11+
- Telegram Bot Token
- OpenAI API Key
- Telegram ID администратора (для функций администратора)

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
# Обязательные переменные
TG_TOKEN=your_telegram_bot_token
OPENAI_API_KEY=your_openai_api_key

# Опциональные переменные (рекомендуется для администратора)
ADMIN_TELEGRAM_ID=your_telegram_user_id
CONFIRMATION_TIMEOUT_HOURS=24

# Дополнительные настройки (опционально)
LLM_MODEL=gpt-4o-mini
FAISS_PATH=./data/index.faiss
CACHE_BACKEND=memory
CACHE_TTL=3600
LOG_LEVEL=INFO
```

**Как получить Telegram User ID:**
1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему команду `/start`
3. Скопируйте ваш ID (число) и используйте его как `ADMIN_TELEGRAM_ID`

## Использование

### Индексация книг

Загрузите книги в папку `data/books/` и выполните индексацию:

```bash
python -m src.main ingest
```

**Формат названий файлов для автоматического определения категорий:**
```
Название книги (категория1, категория2).pdf
Пример: "48 законов власти (психология, социология).fb2"
```

Если категории указаны в названии файла, они будут автоматически извлечены. В противном случае LLM определит категории, и администратор получит запрос на подтверждение.

**Принудительная переиндексация всех файлов:**
```bash
python -m src.main ingest --force
```

**Индексация из другой папки:**
```bash
python -m src.main ingest --folder /path/to/books
```

**Восстановление индекса из метаданных:**
Если FAISS индекс поврежден, его можно восстановить из метаданных без полной переиндексации:
```bash
python -m src.main rebuild-index
```
Эта команда пересоздаст эмбеддинги из текстов чанков, сохраненных в метаданных, что намного быстрее полной переиндексации.

### Запуск бота

```bash
python -m src.main run
```

Бот автоматически:
- Проверяет новые книги каждые 10 минут
- Отправляет уведомления администратору о новых книгах
- Очищает старые запросы при запуске

### Команды бота

#### Для всех пользователей:
- `/start` - Начать работу с ботом, выбрать категории для фильтрации
- `/help` - Показать справку по командам
- `/categories` - Управление выбором категорий для фильтрации поиска

#### Для администратора:
- `/pending` - Показать список книг, ожидающих подтверждения категорий
- `/pending_books` - Показать список непроиндексированных книг
- `/cleanup` - Очистить все старые запросы (обработанные и pending)
- `/cleanup_pending_books` - Полностью очистить список непроиндексированных книг

### Рабочий процесс

1. **Пользователь задает вопрос** → Бот показывает клавиатуру выбора категорий
2. **Пользователь выбирает категории** (можно несколько) → Нажимает "🔍 Начать поиск"
3. **Бот ищет информацию** в выбранных категориях и отвечает
4. **Если информации нет** → Бот предлагает выбрать другие категории или использовать "Все категории"
5. **После ответа** → Кнопка "🔄 Изменить категории" для нового запроса

### Индексация новых книг

**Автоматический режим:**
- Бот проверяет папку `data/books/` каждые 10 минут
- При обнаружении новых книг администратор получает уведомление с кнопками:
  - ✅ **Индексировать** - запустить индексацию
  - ❌ **Отмена** - отложить индексацию
  - 📋 **Показать список** - детальный список книг

**Ручной режим:**
```bash
python -m src.main ingest
```

### Подтверждение категорий

Если LLM не может определить категории из названия файла:
1. Администратор получает уведомление в Telegram
2. Может подтвердить, отклонить или изменить категории
3. После подтверждения книга автоматически индексируется
4. Запросы на подтверждение автоматически удаляются через 24 часа

## Структура проекта

```
ai_library_bot/
├── src/
│   ├── main.py                    # Точка входа
│   ├── config.py                  # Конфигурация и настройки
│   ├── ingest_service.py          # Индексация книг
│   ├── retriever_service.py       # Поиск релевантных фрагментов
│   ├── analyzer.py                # Анализ чанков и генерация ответов
│   ├── telegram_bot.py            # Telegram бот и обработчики
│   ├── formatters.py              # Форматирование сообщений
│   ├── category_parser.py         # Парсинг категорий из названий файлов
│   ├── category_classifier.py     # Определение категорий через LLM
│   ├── confirmation_manager.py    # Управление запросами на подтверждение
│   ├── admin_messages.py          # Форматирование сообщений для администратора
│   ├── admin_utils.py             # Утилиты для администратора
│   ├── user_categories.py         # Управление категориями пользователей
│   ├── query_context.py           # Контекст запросов для изменения категорий
│   ├── pending_books_manager.py   # Управление непроиндексированными книгами
│   ├── cache_utils.py             # Утилиты для работы с кэшем
│   ├── utils.py                   # Общие утилиты
│   └── prompts/
│       └── system_librarian.txt   # Системный промпт для LLM
├── data/
│   ├── books/                     # Папка для книг
│   ├── index.faiss                # FAISS индекс (автоматически)
│   ├── index.metadata.pkl         # Метаданные индекса (автоматически)
│   ├── index.files.pkl            # Информация о файлах (автоматически)
│   ├── library_catalog.txt        # Каталог библиотеки (автоматически)
│   ├── pending_confirmations.json # Запросы на подтверждение (автоматически)
│   ├── pending_books.json         # Непроиндексированные книги (автоматически)
│   └── query_contexts.json        # Контексты запросов (автоматически)
├── docs/                          # Документация
├── logs/                          # Логи приложения
├── tests/                         # Тесты и проверочные скрипты
├── requirements.txt               # Зависимости
├── requirements-dev.txt           # Зависимости для разработки
└── .env.example                   # Шаблон переменных окружения
```

## Особенности реализации

### Инкрементальная индексация
- Использует SHA256 хеши для определения изменений файлов
- Индексирует только новые или измененные файлы
- Автоматически удаляет из индекса удаленные файлы
- Очищает кэш ответов при изменении/удалении книг

### Кэширование
- Кэширует ответы LLM для одинаковых запросов
- Ключ кэша включает текст запроса и выбранные категории
- Автоматическая очистка кэша при изменении книг
- TTL кэша: 1 час (настраивается через `CACHE_TTL`)

### Фильтрация по категориям
- Строгая фильтрация: если в выбранных категориях нет информации, возвращается `NOT_FOUND`
- Пользователь получает информативное сообщение с рекомендациями
- Возможность изменить категории после ответа

### Управление запросами
- Запросы на подтверждение хранятся в JSON файле
- Автоматическая валидация: проверка существования файлов
- Автоматическое удаление старых запросов (старше 1 дня)
- Ручная очистка через команду `/cleanup`

### Каталог библиотеки
- Автоматически создается файл `library_catalog.txt` со списком всех проиндексированных книг
- Книги сгруппированы по категориям
- Содержит общую статистику (количество книг, чанков, книг по категориям)
- Обновляется автоматически при добавлении, удалении или изменении книг

### Восстановление индекса
- При повреждении FAISS индекса система автоматически пытается восстановить его из метаданных
- Восстановление пересоздает эмбеддинги из текстов чанков, сохраненных в `index.metadata.pkl`
- Это намного быстрее полной переиндексации, так как не требуется парсинг файлов и определение категорий
- Ручное восстановление через команду `python -m src.main rebuild-index`

### Ограничения
- Максимальный размер файла: **40 MB**
- Максимальное количество страниц PDF: 1000

## Безопасность

- Все чувствительные данные хранятся в `.env` (не коммитится)
- Используйте `.env.example` как шаблон
- Pre-commit hooks проверяют наличие секретов перед коммитом
- Ограничьте доступ к файлу `.env`: `chmod 600 .env`
- Проверка прав администратора для всех административных команд

## Переменные окружения

### Обязательные
- `TG_TOKEN` - Токен Telegram бота (получить у @BotFather)
- `OPENAI_API_KEY` - Ключ API OpenAI

### Опциональные (рекомендуется)
- `ADMIN_TELEGRAM_ID` - Telegram ID администратора (для уведомлений и команд)
- `CONFIRMATION_TIMEOUT_HOURS` - Таймаут для запросов на подтверждение (по умолчанию: 24)

### Дополнительные настройки
- `LLM_MODEL` - Модель LLM (по умолчанию: `gpt-4o-mini`)
- `FAISS_PATH` - Путь к FAISS индексу (по умолчанию: `./data/index.faiss`)
- `CACHE_BACKEND` - Бэкенд кэша (по умолчанию: `memory`)
- `CACHE_TTL` - TTL кэша в секундах (по умолчанию: `3600`)
- `LOG_LEVEL` - Уровень логирования (по умолчанию: `INFO`)

## Лицензия

MIT


