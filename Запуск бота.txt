cd ai_library_bot
python -m src.main run 

python -m src.main ingest --folder data/books

*Удалить старый индекс 
Remove-Item data\index.faiss -ErrorAction SilentlyContinue
Remove-Item data\index.faiss.metadata.pkl -ErrorAction SilentlyContinue

   * Индексация всех книг
python -m src.main ingest --folder data/books
   *Повторная индексация (файлы не изменились)
python -m src.main ingest --folder data/books
   *Принудительная переиндексация (Переиндексирует все файлы, даже если они не изменились):
python -m src.main ingest --folder data/books --force
   *Проверка индекса файлов
cd ai_library_bot
python check_index.py

python test_encoding.py